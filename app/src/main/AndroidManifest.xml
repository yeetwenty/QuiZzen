<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quizzen"
        tools:targetApi="33">
        <activity
            android:name=".navigation.FavoriteBook"
            android:exported="false" />
        <activity
            android:name=".flashcards.AddFlashcards"
            android:exported="false" />
        <activity
            android:name=".flashcards.ViewFlashcard"
            android:exported="false" />
        <activity
            android:name=".login.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".flashcards.FlashcardSearchActivity"
            android:exported="true" />
        <activity
            android:name=".book.BookRead"
            android:exported="false" />
        <activity
            android:name=".navigation.BottomNavigationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>